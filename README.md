# wlink
wlink is a simple link shortener written in NodeJS, designed to be used with mnemonics. wlink is derived from "word link", which means shortening long urls to memorable words.

<img width="1724" height="1016" alt="image" src="https://github.com/user-attachments/assets/869833d2-f0b8-430f-9860-ff38bc5daa87" />

## Installation

Wlink can be immediately cloned and run using these commands below:
### Linux and Windows:
`
git clone https://github.com/jankoneli/wlink.git && cd wlink && npm i && node index.js
`
### Detached Running (Always On)
Get into your directory where the repository is located, and run `npm i forever -g && forever start index.js` to run detached instance.
